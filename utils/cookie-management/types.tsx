import LocalManagementImpl, {
  LocalManagementOutImpl,
} from "../local-management/types";

export default interface CookieManagementImpl extends LocalManagementImpl {}
export type CookieManagementOutImpl = LocalManagementOutImpl;
